# Weather Forecast Dashboard

Premium, responsive weather dashboard built with React + Vite.

## Setup

1. Install dependencies:

```bash
npm install
```

2. Create `.env` with your OpenWeather key:

```
VITE_OPENWEATHER_KEY=your_api_key_here
```

3. Run dev server:

```bash
npm run dev
```
